[build]
  command = "bun run build"
  # Nitro's netlify preset writes static assets to dist/client and the SSR
  # function to .netlify/functions-internal/server.
  publish = "dist/client"

[build.environment]
  NITRO_PRESET = "netlify"
  NODE_VERSION = "20"
